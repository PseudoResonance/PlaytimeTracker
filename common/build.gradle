dependencies {
	compile group: 'com.google.guava', name: 'guava', version: "${rootProject.guava_version}"
	compile group: 'com.google.code.gson', name: 'gson', version: "${rootProject.gson_version}"
}

task remapJar << {
}
remapJar.dependsOn jar